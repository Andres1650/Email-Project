import { BadgesDirective } from './badges.directive';

describe('BadgesDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgesDirective();
    expect(directive).toBeTruthy();
  });
});
